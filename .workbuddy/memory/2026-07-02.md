# 2026-07-02 工作记录

## 日历功能隐藏（UI 层关闭入口）

用户希望暂时关闭日历功能，采用「隐藏入口」方案（保留代码/数据，方便恢复）。

### 修改文件

1. **NodeEditItem.kt（约 423 行）**
   - 移除「日历」类型 FilterChip，节点类型选择栏现在只剩「便签」
   - 原位置替换为注释 `// 日历类型入口已隐藏（功能暂关闭）`

2. **TreeEditor.kt（约 47、151、169 行）**
   - 注释掉 `showGlobalCalendarEditor` 变量声明
   - 移除顶部工具栏的「全局日历」图标按钮（CalendarMonth 图标）
   - 移除对应的 `CalendarEditorDialog` 弹窗逻辑

### 未改动（数据/逻辑层完整保留）
- `Persistence.kt`：`NodeType.CALENDAR`、`CalendarEvent`、`sharedCalendarEnabled` 等数据结构不变
- `KnowledgeViewModel.kt` / `KnowledgeRepository.kt`：日历业务逻辑不变
- `CalendarEditorDialog.kt`：组件文件保留
- 已有日历节点数据不受影响

### 恢复方式
取消 TreeEditor.kt 和 NodeEditItem.kt 中对应注释即可完整恢复。
